CREATE OR REPLACE VIEW student_ca_marks AS
SELECT 
    rs.reg_num,
    c.course_code,
    rs.ca_marks,
    rs.ca_pass
FROM result_summary rs
JOIN course c ON rs.course_id = c.course_id
ORDER BY rs.reg_num, c.course_code;
